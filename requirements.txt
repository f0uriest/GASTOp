# This is an implicit value, here for clarity
--index-url https://pypi.python.org/simple/

numpy >= 1.13.0
matplotlib >= 2.2.0
configobj >= 5.0.6
tqdm >= 4.28.1
colorama >= 0.4.0
pytest >=3.0.5
pytest-cov
coveralls
codecov
